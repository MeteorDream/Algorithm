---
title: 『LeetCode』1415 长度为 n 的开心字符串中字典序第 k 小的字符串
date: 2026-03-14 13:03:20
updated: 2026-03-14 13:03:20
categories:
- [LeetCode]
tags: [LeetCode]
---
## 题目

[1415. 长度为 n 的开心字符串中字典序第 k 小的字符串](https://leetcode.cn/problems/the-k-th-lexicographical-string-of-all-happy-strings-of-length-n/)

<!--more-->

一个 「开心字符串」定义为：

- 仅包含小写字母 `[&#39;a&#39;, &#39;b&#39;, &#39;c&#39;]`.
- 对所有在 `1` 到 `s.length - 1` 之间的 `i` ，满足 `s[i] != s[i + 1]` （字符串的下标从 1 开始）。

比方说，字符串 **"abc"**，**"ac"，"b"** 和 **"abcbabcbcb"** 都是开心字符串，但是 **"aa"**，**"baa"** 和 **"ababbc"** 都不是开心字符串。

给你两个整数 `n` 和 `k` ，你需要将长度为 `n` 的所有开心字符串按字典序排序。

请你返回排序后的第 k 个开心字符串，如果长度为 `n` 的开心字符串少于 `k` 个，那么请你返回 **空字符串** 。

**示例 1：**

输入：n = 1, k = 3
输出："c"
解释：列表 ["a", "b", "c"] 包含了所有长度为 1 的开心字符串。按照字典序排序后第三个字符串为 "c" 。

**示例 2：**

输入：n = 1, k = 4
输出：""
解释：长度为 1 的开心字符串只有 3 个。

**示例 3：**

输入：n = 3, k = 9
输出："cab"
解释：长度为 3 的开心字符串总共有 12 个 ["aba", "abc", "aca", "acb", "bab", "bac", "bca", "bcb", "cab", "cac", "cba", "cbc"] 。第 9 个字符串为 "cab"

**示例 4：**

输入：n = 2, k = 7
输出：""

**示例 5：**

输入：n = 10, k = 100
输出："abacbabacb"

**提示：**

- `1 <= n <= 10`
- `1 <= k <= 100`

## 标签

字符串, 回溯

---

## 题解

[【长度为 n 的开心字符串中字典序第 k 小的字符串】]()

这是一个数学+构造的问题。我们可以利用开心字符串的规律来直接计算第k个字符串，而不需要生成所有字符串。

### 核心观察

1. 长度为n的开心字符串总数为 3 × 2^(n-1)
    - 第一个字符有3种选择(a, b, c)
    - 后续每个字符有2种选择（不能与前一个字符相同）
2. 可以将所有开心字符串按字典序分组：
    - 以'a'开头的字符串组
    - 以'b'开头的字符串组
    - 以'c'开头的字符串组
3. 每组包含 2^(n-1) 个字符串

### 算法步骤

1. 首先判断k是否超出总数量，如果超出则返回空字符串
2. 确定第一个字符：
    - 每组有 2^(n-1) 个字符串
    - 第k个字符串在第 (k-1) // 2^(n-1) 组中
    - 该组的第一个字符是 'a', 'b', 'c' 中的第 (k-1) // 2^(n-1) 个
3. 对于后续字符，同样使用数学方法确定：
    - 每个位置的可选字符数为2（不能与前一个字符相同）
    - 通过k值和剩余组合数确定当前字符

```Python
# Code language: Python
def getHappyString(n, k):
    # 计算总共有多少个开心字符串
    total = 3 * (2 ** (n - 1))
    
    # 如果k大于总数量，返回空字符串
    if k > total:
        return ""
    
    # 字符集
    chars = ['a', 'b', 'c']
    
    # 结果字符串
    result = []
    
    # 第一个字符的选择
    group_size = 2 ** (n - 1)
    first_char_index = (k - 1) // group_size
    result.append(chars[first_char_index])
    
    # 剩余字符的处理
    k = (k - 1) % group_size
    prev_char = chars[first_char_index]
    
    # 逐个确定后续字符
    for i in range(1, n):
        # 剩余的组合数
        remaining = 2 ** (n - 1 - i)
        
        # 当前字符在剩余字符中的索引
        char_index = k // remaining
        
        # 确保不与前一个字符相同
        if prev_char == 'a':
            # 可选字符是 'b', 'c'
            # 如果char_index是0，选择'b'；如果char_index是1，选择'c'
            current_char = 'b' if char_index == 0 else 'c'
        elif prev_char == 'b':
            # 可选字符是 'a', 'c'
            # 如果char_index是0，选择'a'；如果char_index是1，选择'c'
            current_char = 'a' if char_index == 0 else 'c'
        else:  # prev_char == 'c'
            # 可选字符是 'a', 'b'
            # 如果char_index是0，选择'a'；如果char_index是1，选择'b'
            current_char = 'a' if char_index == 0 else 'b'
        
        result.append(current_char)
        k = k % remaining
        prev_char = current_char
    
    return ''.join(result)
```

```Java
// Code language: Java
public String getHappyString(int n, int k) {
    // 计算总共有多少个开心字符串
    int total = 3 * (int)Math.pow(2, n - 1);
    
    // 如果k大于总数量，返回空字符串
    if (k > total) {
        return "";
    }
    
    // 字符集
    char[] chars = {'a', 'b', 'c'};
    
    // 结果字符串
    StringBuilder result = new StringBuilder();
    
    // 第一个字符的选择
    int groupSize = (int)Math.pow(2, n - 1);
    int firstCharIndex = (k - 1) / groupSize;
    result.append(chars[firstCharIndex]);
    
    // 剩余字符的处理
    k = (k - 1) % groupSize;
    char prevChar = chars[firstCharIndex];
    
    // 逐个确定后续字符
    for (int i = 1; i < n; i++) {
        // 剩余的组合数
        int remaining = (int)Math.pow(2, n - 1 - i);
        
        // 当前字符在剩余字符中的索引
        int charIndex = k / remaining;
        
        // 确保不与前一个字符相同
        char currentChar;
        if (prevChar == 'a') {
            // 可选字符是 'b', 'c'
            // 如果charIndex是0，选择'b'；如果charIndex是1，选择'c'
            currentChar = charIndex == 0 ? 'b' : 'c';
        } else if (prevChar == 'b') {
            // 可选字符是 'a', 'c'
            // 如果charIndex是0，选择'a'；如果charIndex是1，选择'c'
            currentChar = charIndex == 0 ? 'a' : 'c';
        } else {
            // prevChar == 'c'
            // 可选字符是 'a', 'b'
            // 如果charIndex是0，选择'a'；如果charIndex是1，选择'b'
            currentChar = charIndex == 0 ? 'a' : 'b';
        }
        
        result.append(currentChar);
        k = k % remaining;
        prevChar = currentChar;
    }
    
    return result.toString();
}
```

```C++
// Code language: C++
string getHappyString(int n, int k) {
    // 计算总共有多少个开心字符串
    int total = 3 * (1 << (n - 1));
    
    // 如果k大于总数量，返回空字符串
    if (k > total) {
        return "";
    }
    
    // 字符集
    vector<char> chars = {'a', 'b', 'c'};
    
    // 结果字符串
    string result = "";
    
    // 第一个字符的选择
    int groupSize = 1 << (n - 1);
    int firstCharIndex = (k - 1) / groupSize;
    result += chars[firstCharIndex];
    
    // 剩余字符的处理
    k = (k - 1) % groupSize;
    char prevChar = chars[firstCharIndex];
    
    // 逐个确定后续字符
    for (int i = 1; i < n; i++) {
        // 剩余的组合数
        int remaining = 1 << (n - 1 - i);
        
        // 当前字符在剩余字符中的索引
        int charIndex = k / remaining;
        
        // 确保不与前一个字符相同
        char currentChar;
        if (prevChar == 'a') {
            // 可选字符是 'b', 'c'
            // 如果charIndex是0，选择'b'；如果charIndex是1，选择'c'
            currentChar = charIndex == 0 ? 'b' : 'c';
        } else if (prevChar == 'b') {
            // 可选字符是 'a', 'c'
            // 如果charIndex是0，选择'a'；如果charIndex是1，选择'c'
            currentChar = charIndex == 0 ? 'a' : 'c';
        } else {
            // prevChar == 'c'
            // 可选字符是 'a', 'b'
            // 如果charIndex是0，选择'a'；如果charIndex是1，选择'b'
            currentChar = charIndex == 0 ? 'a' : 'b';
        }
        
        result += currentChar;
        k = k % remaining;
        prevChar = currentChar;
    }
    
    return result;
}
```

- 时间复杂度: $O(n)$，只需要遍历字符串长度n次
- 空间复杂度: $O(1)$，只使用了常数额外空间（不包括结果字符串的空间）

---

## 最后

**如果对你有帮助的话，请给我点个赞吧**~

欢迎前往 [我的博客](https://meteordream.github.io/categories/LeetCode/) 或 [Algorithm - Github](https://github.com/MeteorDream/Algorithm) 查看更多题解
